<h1>
  <span class="headline">Intro to Python for Scripting Lab</span>
  <span class="subhead">Rubric</span>
</h1>

## Lab requirements

The following requirements describe the functionality that should be present in the lab and the points awarded out of 100 for each piece of functionality.

| Criteria                                                                                                                                                                                                                                                                                                                                                | Points |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| The <code class="filepath">functions.py</code> script exists.                                                                                                                                                                                                                                                                                           | 6      |
| The <code class="filepath">functions.py</code> script contains a function named `validate_age()` that takes an age as input, raises a `ValueError` if the age is not a positive integer, or returns an age if it is. The function should be documented with a docstring explaining the functions behavior and how to handle the potential `ValueError`. | 7      |
| The <code class="filepath">functions.py</code> script contains a function named `calculate_rectangle_area()` that takes the length and width of a rectangle as arguments and returns the area of a rectangle.                                                                                                                                           | 7      |
| The <code class="filepath">functions.py</code> script contains a function named `calculate_circle_area()` that takes the radius of a circle as an argument and returns the area of the circle.                                                                                                                                                          | 7      |
| The <code class="filepath">functions.py</code> script contains a function named `get_area()` that takes the shape (as a string) and the necessary dimensions as arguments. The function should call the appropriate area calculation function.                                                                                                          | 7      |
| The <code class="filepath">modules.py</code> script exists.                                                                                                                                                                                                                                                                                             | 6      |
| The <code class="filepath">modules.py</code> script contains a function with an appropriate name that generates a random password containing uppercase letters, lowercase letters, and numbers of a specified length over 8 characters.                                                                                                                 | 10     |
| The <code class="filepath">modules.py</code> script contains a function with an appropriate name that takes two strings in the "YYYY-MM-DD" as input, uses the `datetime` module to convert these strings into `datetime` objects, and calculate the difference in days between the two provided dates.                                                 | 10     |
| The <code class="filepath">modules.py</code> script contains a function with an appropriate name that takes a number (the radius of a circle) as an input. It should utilize the `pi` constant from the `math` module to help calculate the circle's area, and return the result.                                                                       | 10     |
| The <code class="filepath">files.py</code> script exists.                                                                                                                                                                                                                                                                                               | 6      |
| The <code class="filepath">files.py</code> script contains a function with an appropriate name that reads a text file (<code class="filepath">sample.txt</code>), counts the number of words in teh file, and handles `FileNotFoundError` gracefully.                                                                                                   | 12     |
| The <code class="filepath">files.py</code> script contains a function with an appropriate name that takes a list of strings and writes each to a new line in a file named <code class="filepath">output.txt</code>.                                                                                                                                     | 12     |
| **Total possible points**                                                                                                                                                                                                                                                                                                                               | 100    |
