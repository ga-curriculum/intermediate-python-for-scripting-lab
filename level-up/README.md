  <h1>
    <span class="headline">Intermediate Python Lab</span>
    <span class="subhead">Level Up</span>
  </h1>

# Optional Exercises

1. Given a list of numbers, use a list comprehension to create a new list containing only the even numbers.  Then, use another list comprehension to square each of the even numbers.

2. Write a function that uses regular expressions to validate a phone number in a specific format (e.g., (XXX) XXX-XXXX).  The function should return True if the phone number is valid, and False otherwise.
